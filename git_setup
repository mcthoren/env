#!/bin/sh

# and some reason, for really old git's
# git config --global alias.lg "log --color --graph --date=iso --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cd) %C(bold blue)<%an>%Creset' --abbrev-commit"

# git config --global user.email XXXXXXXXXX@XXXXXX.XXX
git config --global user.name mcthoren
git config --global push.default simple
git config --global pull.ff only # what do i know? try this for now.
